
  <h1 class="text-center">Create a New Doctor</h1>

  <div class="row">
    <div class="panel panel-default">
      <div class="col-md-5">
        <%= form_for @doctor, class: 'form-horizontal' do |f| %>
        <%= render partial: 'form', locals: { f: f } %>
        <% end %>
      </div>
    </div>

  </div>

<%= link_to '< Go Back', doctors_path %>